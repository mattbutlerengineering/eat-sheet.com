---
phase: 02-guest-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/db/schema.sql
  - src/server/types.ts
  - src/server/routes/guests.ts
  - src/client/pages/Guests.tsx
  - src/client/components/GuestCard.tsx
autonomous: true
requirements:
  - GUEST-08
  - GUEST-09
  - GUEST-10
user_setup: []
must_haves:
  truths:
    - User can see guest VIP tier in guest list and details
    - User can see guest birthday/anniversary in guest profile
    - User can filter guests by allergy tags (allergy:*)
    - User can set guest VIP tier (Regular/VIP/Priority)
    - User can set guest birthday and anniversary dates
  artifacts:
    - path: src/server/db/schema.sql
      contains: vip_tier, birthday, anniversary
    - path: src/server/types.ts
      contains: Guest interface updated with vip_tier, birthday, anniversary
    - path: src/server/routes/guests.ts
      contains: PATCH and POST support vip_tier, birthday, anniversary, tags filtering
    - path: src/client/pages/Guests.tsx
      contains: Display vip_tier badge, birthday/anniversary, filter by allergy:*
  key_links:
    - from: src/server/routes/guests.ts
      to: src/server/db/schema.sql
      via: SQL INSERT/UPDATE with new columns
    - from: src/client/pages/Guests.tsx
      to: src/server/routes/guests.ts
      via: API fetch for guest list with tag filter
---

<objective>
Enhance guest profiles with VIP tiers, special occasions, and allergy tracking.

Purpose: Enable staff to track guest preferences (allergies via tags), VIP status (tiered), and special occasions (birthday/anniversary) for personalized service.

Output: Updated guest schema, API, and UI supporting:
- vip_tier field (Regular/VIP/Priority)
- birthday and anniversary date fields
- Allergy tags searchable via `allergy:*` prefix
</objective>

<context>
@.planning/phases/02-guest-experience/02-CONTEXT.md

From schema.sql:
- guests table already has `tags` column (TEXT)
- Use tags approach for allergies: `allergy:gluten`, `dietary:vegetarian`

From routes/guests.ts:
- Existing pattern: tags stored as comma-separated string
- Search via `LIKE '%tag%'`
- Guest type at src/server/types.ts lines 94-105
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add guest enhancement fields to schema</name>
  <files>src/server/db/schema.sql, src/server/types.ts</files>
  <action>
Add to guests table:
- vip_tier TEXT DEFAULT 'regular' CHECK (vip_tier IN ('regular', 'vip', 'priority'))
- birthday TEXT (ISO date format YYYY-MM-DD)
- anniversary TEXT (ISO date format YYYY-MM-DD)

Also add index on vip_tier for sorting queries.

Update Guest interface in src/server/types.ts:
- Add vip_tier: 'regular' | 'vip' | 'priority'
- Add birthday: string | null
- Add anniversary: string | null
  </action>
  <verify>
grep -n "vip_tier\|birthday\|anniversary" src/server/db/schema.sql
grep -n "vip_tier\|birthday\|anniversary" src/server/types.ts
  </verify>
  <done>Schema has vip_tier, birthday, anniversary columns; types.ts has corresponding fields</done>
</task>

<task type="auto">
  <name>Task 2: Update guest API routes</name>
  <files>src/server/routes/guests.ts</files>
  <action>
Update createGuestSchema and updateGuestSchema to include:
- vip_tier: z.enum(['regular', 'vip', 'priority']).default('regular')
- birthday: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional()
- anniversary: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional()

In PATCH route: allow updating vip_tier, birthday, anniversary fields.

In GET / route: ensure tag filter works with allergy:* pattern (e.g., tag=allergy:gluten).
  </action>
  <verify>
npm test -- --filter=guests 2>/dev/null || echo "No tests found - manual verify via API"
  </verify>
  <done>API accepts and returns vip_tier, birthday, anniversary in guest CRUD operations</done>
</task>

<task type="auto">
  <name>Task 3: Update client UI for guest enhancements</name>
  <files>src/client/pages/Guests.tsx</files>
  <action>
Update GuestCard or guest list display to show:
- VIP tier badge (color-coded: Regular=gray, VIP=gold, Priority=purple)
- Birthday icon if set (with date)
- Anniversary icon if set (with date)

Add tag filter input for allergy:* search:
- When user types in search with "allergy:" prefix, call API with tag= parameter

Example UI:
- Show crown icon for VIP/Priority guests
- Show cake icon for birthday (within 7 days)
- Show heart icon for anniversary (within 7 days)
  </action>
  <verify>
npm run build 2>&1 | head -20
  </verify>
  <done>Guest list shows VIP badges, special occasion icons, and supports allergy tag filtering</done>
</task>

</tasks>

<verification>
- [ ] Schema migration can be created (or schema.sql updated)
- [ ] Guest API handles vip_tier, birthday, anniversary in create/update
- [ ] Guest API supports tag filter for allergies (allergy:*)
- [ ] Client displays VIP tier visually
- [ ] Client shows birthday/anniversary when set
- [ ] Client can filter by allergy tags
</verification>

<success_criteria>
Guest profiles support:
- vip_tier field (Regular/VIP/Priority) in CRUD
- birthday and anniversary date fields in CRUD  
- Tags filterable by exact match (e.g., allergy:gluten)
- UI displays VIP status and upcoming special occasions
</success_criteria>

<output>
After completion, create `.planning/phases/02-guest-experience/02-01-SUMMARY.md`
</output>